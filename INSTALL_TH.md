# 🥒 คู่มือติดตั้ง PickleChart สำหรับมือใหม่ (ไม่ต้องเขียนโค้ดเป็น)

คู่มือนี้พาคุณติดตั้ง **PickleChart** ลงเครื่องตัวเอง แล้วเปิดใช้งานในเบราว์เซอร์ โดยให้ AI ฟรีอย่าง **Google Antigravity** ทำงานส่วนที่เป็นโค้ดให้ทั้งหมด คุณแค่คัดลอกข้อความไปวาง แล้วกดอนุญาตเท่านั้น

> ⏱️ ใช้เวลาประมาณ 15–30 นาที (ส่วนใหญ่คือรอดาวน์โหลด)
> 💻 ใช้ได้ทั้ง Windows และ macOS

---

## 📋 สารบัญ

1. [ติดตั้งโปรแกรมที่ต้องใช้ (ทำครั้งเดียว)](#1-ติดตั้งโปรแกรมที่ต้องใช้-ทำครั้งเดียว)
2. [ให้ AI ดาวน์โหลดโปรเจกต์ (Clone)](#2-ให้-ai-ดาวน์โหลดโปรเจกต์-clone)
3. [ให้ AI เปิดโปรแกรม (Run Dev)](#3-ให้-ai-เปิดโปรแกรม-run-dev)
4. [ให้ AI แก้ไข/ปรับแต่งโปรแกรม](#4-ให้-ai-แก้ไขปรับแต่งโปรแกรม)
5. [เปิดใช้ครั้งถัดไป / อัปเดตเวอร์ชันใหม่](#5-เปิดใช้ครั้งถัดไป--อัปเดตเวอร์ชันใหม่)
6. [แก้ปัญหาที่พบบ่อย](#6-แก้ปัญหาที่พบบ่อย)
7. [คำศัพท์ที่ควรรู้](#7-คำศัพท์ที่ควรรู้)

---

## 1. ติดตั้งโปรแกรมที่ต้องใช้ (ทำครั้งเดียว)

ต้องมี 3 โปรแกรม ทั้งหมดฟรี ดาวน์โหลดแล้วกด **Next** ไปเรื่อยๆ ตามค่าเริ่มต้นได้เลย

| โปรแกรม | มีไว้ทำอะไร | ดาวน์โหลด |
| :--- | :--- | :--- |
| **Node.js** (เลือกรุ่น **LTS**) | ตัวรันโปรแกรม PickleChart | https://nodejs.org |
| **Git** | ตัวดาวน์โหลดโค้ดจาก GitHub | https://git-scm.com/downloads |
| **Google Antigravity** | โปรแกรมแก้โค้ดที่มี AI ช่วย (ฟรี) | https://antigravity.google |

> 💡 **สำคัญ:** ติดตั้ง Node.js และ Git **ก่อน** แล้วค่อยเปิด Antigravity
> ถ้าเปิด Antigravity ค้างไว้อยู่แล้ว ให้ปิดแล้วเปิดใหม่ 1 ครั้ง ไม่อย่างนั้นมันจะมองไม่เห็นโปรแกรมที่เพิ่งติดตั้ง

**ตั้งค่า Antigravity ครั้งแรก**
1. เปิด Antigravity แล้วล็อกอินด้วยบัญชี Google
2. ถ้าถามเรื่องการตั้งค่า ใช้ค่าที่แนะนำไปได้เลย
3. สร้างโฟลเดอร์ว่างไว้เก็บโปรเจกต์ เช่น `Documents\Projects`
4. ใน Antigravity ไปที่ **File → Open Folder** แล้วเลือกโฟลเดอร์นั้น
5. เปิดหน้าต่าง **Agent** (หน้าต่างแชทกับ AI ปกติจะอยู่ด้านขวา) คุณจะพิมพ์คำสั่งทั้งหมดในหน้าต่างนี้

---

## 2. ให้ AI ดาวน์โหลดโปรเจกต์ (Clone)

คัดลอกข้อความในกล่องด้านล่าง ไปวางในแชท Agent แล้วกด Enter:

```text
ช่วยตรวจสอบก่อนว่าเครื่องนี้มี Node.js (เวอร์ชัน 18 ขึ้นไป) และ Git แล้วหรือยัง
ถ้ามีครบแล้ว ให้ clone repo นี้ลงในโฟลเดอร์ที่เปิดอยู่:
https://github.com/ikarisz/PickleChart.git
จากนั้นเข้าไปในโฟลเดอร์ PickleChart แล้วรัน npm install
อธิบายทุกขั้นตอนเป็นภาษาไทยง่ายๆ เพราะฉันเขียนโค้ดไม่เป็น
```

**สิ่งที่จะเกิดขึ้น**
- AI จะขออนุญาตรันคำสั่ง (เช่น `git clone`, `npm install`) ให้กด **Accept / Run** ได้เลย
- `npm install` จะดาวน์โหลดไฟล์ประกอบประมาณ 1–3 นาที เห็นตัวหนังสือวิ่งเยอะๆ ถือว่าปกติ
- ขึ้นคำว่า `WARN` (สีเหลือง) ไม่ต้องตกใจ ไม่ใช่ข้อผิดพลาด ที่ต้องสนใจคือ `ERR!` (สีแดง) เท่านั้น

> 🔒 **ความปลอดภัย:** อ่านคร่าวๆ ทุกครั้งก่อนกดอนุญาต คำสั่งในขั้นนี้ควรมีแค่ `git`, `npm`, `node`, `cd` ถ้า AI ขอลบไฟล์นอกโปรเจกต์หรือขอรหัสผ่าน ให้กดปฏิเสธ

เมื่อเสร็จแล้ว ไปที่ **File → Open Folder** แล้วเลือกโฟลเดอร์ `PickleChart` ที่เพิ่งดาวน์โหลดมา เพื่อให้ AI ทำงานในโปรเจกต์นี้โดยตรง

---

## 3. ให้ AI เปิดโปรแกรม (Run Dev)

วางข้อความนี้ในแชท Agent:

```text
ช่วยรัน npm run dev ให้หน่อย แล้วบอกลิงก์ที่ต้องเปิดในเบราว์เซอร์
ถ้ามี error ช่วยอ่านแล้วแก้ให้ด้วย
```

เมื่อรันสำเร็จจะเห็นข้อความประมาณนี้:

```text
  VITE v6.x.x  ready in 500 ms

  ➜  Local:   http://localhost:3000/
```

เปิดเบราว์เซอร์ (Chrome, Edge ฯลฯ) แล้วเข้า **http://localhost:3000** จะเห็นหน้าจอ Windows XP พร้อมกราฟ PickleChart 🎉

**ข้อควรรู้**
- **ห้ามปิด** หน้าต่าง Terminal ที่รันอยู่ ถ้าปิด โปรแกรมจะหยุดทำงาน
- ถ้าต้องการหยุด: คลิกที่ Terminal แล้วกด `Ctrl + C`
- `localhost` คือเครื่องของคุณเอง ไม่ได้ขึ้นเว็บให้คนอื่นเห็น
- กราฟดึงราคาสดจาก Binance ต้องต่ออินเทอร์เน็ตอยู่เสมอ

---

## 4. ให้ AI แก้ไข/ปรับแต่งโปรแกรม

ข้อดีของการรันเองคือคุณสั่งให้ AI ปรับโปรแกรมตามใจได้เลย ระหว่างที่ `npm run dev` ยังรันอยู่ แก้ไฟล์เมื่อไหร่หน้าเว็บจะอัปเดตเองภายในไม่กี่วินาที

### ✍️ ตัวอย่างคำสั่งที่ใช้ได้เลย

```text
อยากให้เปลี่ยนสีแท่งเทียนขาขึ้นเป็นสีฟ้า และขาลงเป็นสีส้ม ช่วยหาไฟล์ที่เกี่ยวข้องแล้วแก้ให้
```

```text
อยากเพิ่มเหรียญ DOGEUSDT ในรายการสินทรัพย์ ช่วยดูว่าเหรียญอื่นถูกเพิ่มไว้ที่ไหน แล้วเพิ่มแบบเดียวกัน
```

```text
ช่วยเพิ่มเส้น EMA 100 ในตัวเลือก indicator
```

```text
ช่วยอธิบายโครงสร้างโปรเจกต์นี้แบบง่ายๆ ว่าแต่ละโฟลเดอร์ทำหน้าที่อะไร
```

```text
หน้าเว็บขึ้นจอขาว ช่วยดู error ใน terminal แล้วแก้ให้หน่อย
```

### 🧠 เทคนิคสั่ง AI ให้ได้ผลดี

1. **สั่งทีละเรื่อง** แก้เสร็จเรื่องหนึ่ง เช็กในเบราว์เซอร์ แล้วค่อยสั่งเรื่องถัดไป
2. **บอกสิ่งที่เห็น ไม่ต้องเดาโค้ด** เช่น "ปุ่มตั้งค่ามุมขวาบนกดแล้วไม่มีอะไรขึ้น"
3. **ถ้ามี error** ให้คัดลอกข้อความสีแดงทั้งหมดไปวางให้ AI
4. **แนบรูปหน้าจอ** ได้ ถ้าอธิบายด้วยคำพูดยาก
5. **ให้ AI ตรวจงานตัวเอง** ต่อท้ายคำสั่งว่า "แก้เสร็จแล้วรัน npm run build เพื่อเช็กว่าไม่มี error"

### ↩️ ถ้าแก้แล้วพัง อยากย้อนกลับ

```text
ช่วยย้อนการแก้ไขทั้งหมดที่ยังไม่ได้ commit กลับไปเป็นเหมือนตอนดาวน์โหลดมาครั้งแรก
```

หรือถ้าอยากเก็บงานที่แก้ไว้เป็นจุด save:

```text
ช่วย git commit การแก้ไขตอนนี้ไว้ พร้อมเขียนข้อความสั้นๆ ว่าแก้อะไรไป
```

> 💾 **หมายเหตุ:** ค่าตั้งค่า เส้นที่วาด และ preset ของคุณเก็บในเบราว์เซอร์ (localStorage) ไม่ได้อยู่ในไฟล์โค้ด ย้อนโค้ดแล้วค่าเหล่านี้จะไม่หาย แต่ถ้าล้างข้อมูลเบราว์เซอร์จะหาย ควรใช้ **Export Preset** เก็บเป็นไฟล์ไว้

---

## 5. เปิดใช้ครั้งถัดไป / อัปเดตเวอร์ชันใหม่

### เปิดใช้งานครั้งถัดไป
1. เปิด Antigravity → **File → Open Folder** → เลือกโฟลเดอร์ `PickleChart`
2. สั่ง AI ว่า `ช่วยรัน npm run dev ให้หน่อย` (หรือพิมพ์ `npm run dev` ใน Terminal เอง)
3. เปิด http://localhost:3000

### ดึงเวอร์ชันใหม่จาก GitHub

```text
ช่วยอัปเดตโปรเจกต์นี้เป็นเวอร์ชันล่าสุดจาก GitHub (git pull) แล้วรัน npm install ใหม่
ถ้าชนกับส่วนที่ฉันแก้ไว้ ช่วยอธิบายก่อนว่าจะเลือกเก็บอะไร อย่าเพิ่งลบของฉัน
```

---

## 6. แก้ปัญหาที่พบบ่อย

| อาการ | สาเหตุ | วิธีแก้ |
| :--- | :--- | :--- |
| `'node' is not recognized` หรือ `'npm' is not recognized` | ยังไม่ได้ติดตั้ง Node.js หรือเปิด Antigravity ค้างไว้ก่อนติดตั้ง | ติดตั้ง Node.js LTS แล้ว **ปิด Antigravity แล้วเปิดใหม่** |
| `'git' is not recognized` | ยังไม่ได้ติดตั้ง Git | ติดตั้ง Git แล้วปิด-เปิด Antigravity ใหม่ |
| Windows ขึ้นว่า `npm.ps1 cannot be loaded because running scripts is disabled` | PowerShell ของ Windows บล็อกสคริปต์ | ใช้ `npm.cmd install` และ `npm.cmd run dev` แทน หรือสั่ง AI ว่า "ใช้ npm.cmd แทน npm" |
| `Port 3000 is in use` หรือลิงก์เป็น `:3001` | มีโปรแกรมอื่นใช้พอร์ต 3000 อยู่ | ใช้ลิงก์ที่ Terminal แสดงแทน (เช่น http://localhost:3001) |
| เปิดเว็บแล้วขึ้น "This site can't be reached" | ยังไม่ได้รัน `npm run dev` หรือปิด Terminal ไปแล้ว | รัน `npm run dev` ใหม่ แล้วรีเฟรชหน้าเว็บ |
| หน้าเว็บขึ้นแต่กราฟไม่มีข้อมูล | เน็ตหลุด หรือเครือข่ายบล็อกการเชื่อมต่อ Binance | เช็กอินเทอร์เน็ต ลองเปลี่ยนเครือข่าย/ปิด VPN แล้วรีเฟรช |
| หน้าเว็บขาวทั้งหน้าหลังให้ AI แก้โค้ด | โค้ดที่แก้มี error | กด `F12` → แท็บ **Console** คัดลอกข้อความสีแดงไปให้ AI แก้ หรือสั่งย้อนกลับตาม[ข้อ 4](#️-ถ้าแก้แล้วพัง-อยากย้อนกลับ) |
| `npm install` ขึ้น `ERR!` | เน็ตไม่เสถียร หรือ Node.js เก่าเกินไป | สั่ง AI ว่า "ลบโฟลเดอร์ node_modules แล้ว npm install ใหม่" และเช็กว่า Node.js เป็นเวอร์ชัน 18 ขึ้นไป |

> ถ้าแก้ไม่ได้จริงๆ ให้คัดลอก error ทั้งหมดไปถาม AI พร้อมบอกว่าทำขั้นตอนไหนอยู่ หรือเปิด Issue ที่ https://github.com/ikarisz/PickleChart/issues

---

## 7. คำศัพท์ที่ควรรู้

| คำ | ความหมายแบบง่าย |
| :--- | :--- |
| **Repository (Repo)** | โฟลเดอร์โปรเจกต์ที่เก็บไว้บน GitHub |
| **Clone** | ดาวน์โหลดโปรเจกต์จาก GitHub มาไว้ในเครื่อง |
| **npm install** | ดาวน์โหลดชิ้นส่วนที่โปรแกรมต้องใช้ (จะได้โฟลเดอร์ `node_modules`) |
| **npm run dev** | เปิดโปรแกรมแบบนักพัฒนา แก้โค้ดแล้วเห็นผลทันที |
| **Terminal** | หน้าต่างพิมพ์คำสั่ง (ตัวหนังสือบนพื้นดำ) |
| **localhost** | ที่อยู่เว็บที่ชี้มาที่เครื่องของคุณเอง |
| **Commit** | จุด save ของโค้ด ย้อนกลับมาได้ภายหลัง |
| **Git pull** | ดึงโค้ดเวอร์ชันล่าสุดจาก GitHub |

---

<div align="center">

ขอให้สนุกกับการเทรดและการปรับแต่ง 🥒📈
ถ้าแก้อะไรเจ๋งๆ ได้ ส่ง Pull Request มาแบ่งปันกันได้เลย!

</div>
